# Frontend

React + Vite frontend for the Outbox Labs email scheduler.

Run it from this folder with:

```powershell
npm.cmd run dev
```

See the root `README.md` for the full setup and backend flow.
